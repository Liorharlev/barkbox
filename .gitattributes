# Normalise line endings. The repo is edited on Windows but runs on a Linux Pi;
# shell scripts and the systemd unit must stay LF.
* text=auto eol=lf
*.png binary
*.jpg binary
*.mp3 binary
*.wav binary
